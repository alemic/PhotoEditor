<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/crop"
          android:icon="@android:drawable/ic_menu_crop"
          android:title="@string/menu_optins_unCrop"
          android:showAsAction="ifRoom|withText"></item>
        <item android:id="@+id/menu"
                android:title="@string/menu_options_menu"
                android:icon="@android:drawable/ic_menu_view"
                android:showAsAction="ifRoom|withText"></item>
    <item android:icon="@android:drawable/ic_menu_save"
          android:title="@string/save_options_menu"
          android:id="@+id/save"
          android:showAsAction="ifRoom|withText"></item>
</menu>